<%- include('header'); -%>

<% for(var i = 0; i< data.length; i++) { %>

    <tr>
        <th scope="row"><%= (i+1) %></th>
        <td><%= data[i].domain%></td>
        <td><%= data[i].access_token%></td>
    </tr>

<% } %>    

<%- include('footer'); -%>
